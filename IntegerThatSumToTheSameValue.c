// Q.:- Alice has a positive integer N. She is wondering how many ordered pairs of positive integers (i,j) exist such that i + j = N.
// Help Alice figure out the answer.

// Note that since the pairs are ordered, (1,2) and (2,1) are considered different.

#include <stdio.h>

int main(void) 
{
	int a;
	scanf("%d",&a);
	printf("%d",a-1);
	return 0;
}

